// #ifndef DEFAULTPOLLER_H

// #define DEFAULTPOLLER_H





// #endif

#include"eventloop.h"

#include"poller.h"
#include"epollpoller.h"
// class Poller;

// Poller* Poller::newDefaultPoller(EventLoop *loop){
//     return new EpollPoller(loop);
// }